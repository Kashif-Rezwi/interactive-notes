# Concept Model Records

Store source-grounded concept models produced by governed work. Use `templates/concept/concept-model.md`. Each model links its source package, anchors every atomic claim, and declares its domain-review status and confidence.
