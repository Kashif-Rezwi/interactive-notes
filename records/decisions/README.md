# Decision Register Records

This optional directory may hold exports or a searchable register of decision metadata. The full canonical decision narrative is always the numbered ADR in `docs/adr/`; do not create a competing source of truth here.

A generation run's release, hold, revise, or reject decision belongs in that run's `Decision and approvers` section. Do not create a separate release-decision record unless a future workflow explicitly requires one.
