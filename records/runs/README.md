# Generation Run Records

Store one immutable ledger per governed generation or revision run. Use `templates/run/generation-run.md`. Each run links its parent run, inputs, candidates, evaluations, decision, and memory disposition.
